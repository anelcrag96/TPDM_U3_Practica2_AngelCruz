<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.ittepic.anelcruzag.tpdm_u3_practica2_angelcruz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegistroAplicaciones"></activity>
        <activity android:name=".RegistroAlumnos" />
        <activity android:name=".DatosAplicaciones" />
        <activity android:name=".DatosAlumnos" />
        <activity android:name=".ListadoAplicaciones" />
        <activity android:name=".ListadoAlumnos" />
        <activity android:name=".Principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>